name: 🛠️ Fix Code Formatting

on: workflow_dispatch

permissions:
  contents: write

jobs:
  auto-format:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 Instalar Black
        run: pip install black

      - name: 🎨 Ejecutar Formateo (Black)
        # Esto usará la configuración de 100 caracteres del pyproject.toml
        run: black src/ scripts/

      - name: 💾 Guardar Cambios
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖"