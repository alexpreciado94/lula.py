name: 🤖

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: 'pip'

      - name: 📦 Install Black
        run: pip install black

      - name: 🎨 Format code
        run: black . --line-length 100 --exclude '/(venv|build|dist|libs)/'

      - name: 💾 Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖"
          # Aquí forzamos que el nombre sea solo el emoji
          commit_user_name: "🤖"
          commit_user_email: "41898282+github-actions[bot]@://github.com"
          commit_author: "🤖 <41898282+github-actions[bot]@://github.com>"